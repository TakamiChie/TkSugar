_Tk:
  title: "Notebook TEST"
  geometry: 300x400
  _Notebook:
    pack: {expand: True, fill: both}
    ::children:
      - _Frame:
          /text: Tab 1
          ::children:
            !include notebook_tab1.yml
      - _Frame:
          /text: Tab 2
          ::children:
            !include notebook_tab2.yml
